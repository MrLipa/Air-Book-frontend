get:
  tags:
    - Auth
  summary: Refresh access token
  parameters:
    - name: Authorization
      in: header
      required: true
      schema:
        type: string
      example: Bearer dummy_refresh_token
  responses:
    '200':
      description: Token refreshed successfully
    '403':
      description: Invalid or expired token
